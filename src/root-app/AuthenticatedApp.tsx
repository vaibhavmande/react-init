import React from 'react';
import { AuthenticatedAppRoutes } from '@/core/routes/AuthenticatedAppRoutes';

function AuthenticatedApp() {
  return <AuthenticatedAppRoutes />;
}

export default AuthenticatedApp;
